{% extends 'base.html.twig' %}

{% block title %}{{ article.title | upper }}{% endblock %}

{% block body %}
    <h1>{{ article.title }}</h1>
    <p>Écrit le {{ article.createdAt | date('d/m/Y') }}</p>
    <p>{{ article.body }}</p>
{% endblock %}
