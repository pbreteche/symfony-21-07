{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
    <h1>Ça marche !</h1>
    {# ce commentaire twig ne sera pas visible dans le source de la page #}
    <ul>
    {% for id in ids %}
    <li><a href="{{ path('app_default_show', {id: id}) }}">Article n° {{ id }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
          color: #222;
        }
    </style>
{% endblock %}
