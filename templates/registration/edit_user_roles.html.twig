{% extends 'base.html.twig' %}

{% block title %}Gestion des rôles de {{ user.userIdentifier }}{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(form) }}
    {{ form_row(form.roles) }}

    <button type="submit" class="btn">Mettre à jour</button>
    {{ form_end(form) }}
{% endblock %}
